{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lean-server.local/protocol/v2/result.schema.json",
  "$defs": {
    "position": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["line", "column"],
      "properties": {
        "line": {"type": "integer", "minimum": 1},
        "column": {"type": "integer", "minimum": 0}
      }
    },
    "diagnostic": {
      "type": "object",
      "additionalProperties": false,
      "required": ["severity", "message", "file_name", "start", "end"],
      "properties": {
        "severity": {"enum": ["warning", "error"]},
        "message": {"type": "string"},
        "file_name": {"type": ["string", "null"]},
        "start": {"$ref": "#/$defs/position"},
        "end": {"$ref": "#/$defs/position"}
      }
    }
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocol_version",
    "type",
    "request_id",
    "status",
    "compile_ms",
    "formal_statement_ms",
    "candidate_ms",
    "declarations_ms",
    "warnings",
    "errors",
    "tool_errors",
    "failed_declarations"
  ],
  "properties": {
    "protocol_version": {"const": 2},
    "type": {"const": "verify_result"},
    "request_id": {"type": "string", "minLength": 1},
    "status": {"enum": ["ok", "compile_error", "internal_error"]},
    "compile_ms": {"type": "number", "minimum": 0},
    "formal_statement_ms": {"type": "number", "minimum": 0},
    "candidate_ms": {"type": "number", "minimum": 0},
    "declarations_ms": {"type": "number", "minimum": 0},
    "warnings": {"type": "array", "items": {"$ref": "#/$defs/diagnostic"}},
    "errors": {"type": "array", "items": {"$ref": "#/$defs/diagnostic"}},
    "tool_errors": {"type": "array", "items": {"type": "string"}},
    "failed_declarations": {"type": "array", "items": {"type": "string"}}
  }
}
