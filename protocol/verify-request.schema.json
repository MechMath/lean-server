{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lean-server.local/protocol/verify-request.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocol_version",
    "type",
    "request_id",
    "formal_statement",
    "content"
  ],
  "properties": {
    "protocol_version": {"const": 2},
    "type": {"const": "verify"},
    "request_id": {"type": "string", "minLength": 1},
    "formal_statement": {"type": "string", "description": "Must introduce verifiable declarations; complete proofs without sorry are supported."},
    "content": {"type": "string"},
    "use_def_eq": {"type": "boolean", "default": true, "description": "Definitional equality when true; structural expression equality after universe-parameter normalization when false."}
  }
}
