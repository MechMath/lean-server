{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lean-server.local/protocol/ready.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["protocol_version", "type", "lean_version"],
  "properties": {
    "protocol_version": {"const": 2},
    "type": {"const": "ready"},
    "lean_version": {"const": "4.30.0"}
  }
}
