# 219 service validation — 2026-09-21

## Scope

The user requested fixes and remote validation on `159.226.47.219`, using the
existing service in `tmux 17`. Invalid reference extraction is excluded at the
user's request: 591 such records in `axle_failed__local_passed`, plus 107 in
`axle_failed__local_error`, total 698. Original archive files are unchanged.

## Service fixes

- Strict verification checks the reference declaration and dependency policy,
  including generated implementation dependencies of fixed recursive definitions.
- Worker stdout accepts up to 8 MiB, and stderr draining retains a bounded tail.
- Compiler diagnostics are deduplicated only when severity, text, filename,
  start and end position all match. Historical UUID
  `0ae3c609-56ae-5904-9cf3-51d4cb9d0221` previously produced 197,802 identical
  errors and 27,494,706 worker-response bytes. The patched HTTP response is
  approximately 376 bytes with one `No goals to be solved` error. Distinct
  source positions remain distinct diagnostics.
- The known `Nat.pow exponent is too big` panic returns HTTP 503 with
  `error_type: "LeanPanic"`, `retryable: false`. Its worker is replaced. This
  mitigates crash handling; the underlying Lean 4.30 panic remains unresolved.
- Deadlines include queue wait; queued expiry/cancellation releases capacity.
  Cleanup and replacement startup preserve worker-slot ownership.
- At the user's request, strict verification now defaults to a 600-second
  total deadline and accepts at most 600 seconds. The notebook sends 600 and
  allows 615 seconds on the client. Compilation-only `/check` retains its
  documented 30-second default and 120-second maximum.

## Validation

The isolated release at `/home/itbk/lean-server-releases/20260921-verify` uses a
full copy of the Lean environment, built from the current source overlay.
The original service on port 9123 remained running throughout staging validation.

`PYTHONPATH=src .venv/bin/python -m unittest discover -s tests -v` passed all
102 tests in 70.403 seconds on 219. This includes the real Lean worker/verifier,
CLI backend, protocol, queue/recovery, process transport, and HTTP tests.
The earlier local CLI discovery stall was sandbox-specific: both CLI tests
passed outside the local sandbox and in the complete remote suite.
After the timeout change, the full suite passed again: **103 tests in 70.408
seconds**, including acceptance of a 600-second verification budget, rejection
of invalid/out-of-range budgets, and the separate compilation timeout bound.

Staging uses localhost port 19123. Initial regression checks used 16 workers;
the complete replay uses 64 workers and queue capacity 128 on the otherwise
lightly loaded 512-CPU host. Production retains 32 workers and queue capacity 64.
Both the oversized-diagnostic example and actual archived panic were reproduced
over HTTP; their responses match the contracts described above.

Source overlay used for the full 120-second replay, SHA-256:
`0fae420c18631515848e544f21a6dcce8f7800ebff1b92735aa54fe1fc530f81`.
Final overlay with the 600-second HTTP setting, test, and replay option:
`90871aa3961a8836885186c4b20186957405f4fc46aea2ddc9e3172b04ee2e39`.
This subsequent change does not alter Lean verification or the worker binary.
Remote worker SHA-256:
`14f950dc502ee55dd1f25ddc2e8de2a4001f6d2b19b322d4da817196be5e07b7`.

## Historical replay

`replay_http.py` records full compressed responses and
an index, metadata, and summary for each mode. It uses concurrency 64, a
120-second server deadline, and no retries. Both modes use
`--exclude-formal-errors`:

- `check-errors`: 2,951 historical local infrastructure failures after exclusions.
- `verify-all`: 4,228 disagreement records after exclusions.

The compilation replay completed in 260.134 seconds: 500 passed, 2,450 rejected,
and one known nonretryable `LeanPanic`. All 487 historical AXLE-passed/local-error
records now compile successfully. There were no transport errors or timeouts;
the largest HTTP response was 306,171 bytes. Thus all 2,950 included historical
NDJSON failures now return a normal compilation result. The 107 excluded
NDJSON cases with invalid references were not rerun.

Strict verification completed in 360.783 seconds with 568 passed, 3,657
rejected, two timeouts, and the same known panic. No transport failures occurred;
the largest response was 307,050 bytes.

| Historical category | Passed | Rejected | Timeout | Lean panic |
| --- | ---: | ---: | ---: | ---: |
| AXLE failed / local passed (after exclusions) | 4 | 1,194 | 0 | 0 |
| AXLE failed / local error (after exclusions) | 0 | 2,463 | 0 | 1 |
| AXLE passed / local failed | 77 | 0 | 2 | 0 |
| AXLE passed / local error | 487 | 0 | 0 | 0 |

The four acceptances in the first row are the same two historical AXLE
timeouts and two compatibility differences documented in
[the earlier review](axle-failed-local-passed-review.md#remaining-compatibility-cases).
The 13 candidates which now compile but historically failed AXLE are all
rejected by strict verification.

The two local timeouts are `Goedel-Pset-1539351` and `Goedel-Pset-101720`.
The original AXLE timings were 246.784 and 319.105 seconds respectively,
already exceeding the original local 120-second cap. A separate 600-second
HTTP replay of these two records completed at concurrency two against four
staging workers. Both passed: 419.381 seconds for `Goedel-Pset-1539351` and
596.538 seconds for `Goedel-Pset-101720`. The latter is close to the 600-second
limit and can still time out under additional load. Initial results remain
intact in `data/remote-20260921`; follow-up results are stored separately in
`data/remote-20260921-600s`.

Combining the initial run with these explicit follow-ups gives **570 passed,
3,657 rejected, and one known Lean panic**, covering all 4,228 included UUIDs.
All 566 historical AXLE-passed disagreement records now pass strict verification.
There are no unresolved timeouts or transport errors in the combined results.
The panic is still an infrastructure outcome, not a proof rejection.

## Deployment

Completed at 2026-09-21 16:42:54 Asia/Shanghai. The public Base URL remains
`http://159.226.47.219:9123`, running in **tmux `17:1.0`** with 32 ready workers
and queue capacity 64. The deployed project is `/home/itbk/lean-server`.

The previous project and build are preserved at
`/home/itbk/lean-server-releases/20260921-before-update`.
Only the Lean service pane was restarted. Its initial post-deployment check
reported zero worker replacements and no active/queued requests.

Post-deployment HTTP checks accepted a valid proof and rejected a changed
statement, `sorry`, `native_decide`'s extra axiom, and a changed recursive
definition. The local `demo.ipynb` then ran all seven code cells successfully
through the installed `Python (lean-server)` kernel against the original public
Base URL. Sections 5 and 6 executed verification without 404 or skipped checks;
fresh outputs are saved and the notebook remains ignored by Git.

Evidence:

- [Combined replay summary](data/remote-20260921/combined-summary.json)
- [Compilation replay summary](data/remote-20260921/check-errors-summary.json)
- [Initial verification summary](data/remote-20260921/verify-all-summary.json)
- [600-second follow-up summary](data/remote-20260921-600s/verify-all-summary.json)
- [103-test log](data/remote-20260921/logs/lean-server-20260921-tests-final.log)
- [Deployment and HTTP smoke results](data/remote-20260921/deployment.json)

The adjacent index JSONL files contain each UUID's outcome and diagnostics;
the `*-responses.jsonl.gz` files preserve full HTTP responses. These detailed
outputs and intermediate logs are local artifacts ignored by Git; summaries,
metadata, deployment evidence, and the final test log are kept. The full replay
was run before the timeout-only HTTP configuration change; the worker binary
is identical, and the two affected cases were explicitly rerun afterward.

For rollback, stop the service in `17:1.0`, move the current project into an
unused release directory, and restore `20260921-before-update` to
`/home/itbk/lean-server`. From that restored directory, start the original
configuration in the same pane:

```bash
env PYTHONPATH=src .venv/bin/python -m lean_server \
  --host 0.0.0.0 --port 9123 --workers 32 --queue-capacity 64 \
  --worker-startup-timeout 180 --worker-startup-parallelism 16 \
  --worker-command 'lake env .lake/build/bin/lean-server-worker'
```
